# Auto-approve new PRs https://github.com/marketplace/actions/auto-approve
name: Auto approve
on: pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    if: github.actor == 'github-actions[bot]'
    steps:
      - uses: hmarr/auto-approve-action@v3
